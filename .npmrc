# @logto/rn 1.2.0 supports the current native runtime, but its published
# Expo peer range stops before SDK 57. Keep installs reproducible until the
# upstream peer metadata is widened.
legacy-peer-deps=true

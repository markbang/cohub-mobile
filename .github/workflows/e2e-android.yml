name: E2E Android

# The device run installs a released APK and lets production OTA deliver the current
# JS, so it only makes sense once Publish OTA has published that commit. It therefore
# chains off Publish OTA instead of running on every pull request, where the JS under
# test would not be on any channel yet.
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Publish OTA"]
    types: [completed]
    branches: [main]

permissions:
  contents: read

concurrency:
  group: e2e-android
  cancel-in-progress: true

env:
  CI: "true"
  EXPO_NO_TELEMETRY: "1"

jobs:
  android:
    name: Android device E2E
    # A manual staging dispatch publishes a bundle no released binary can fetch.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push')
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      EXPO_PUBLIC_UPDATES_URL: ${{ vars.EXPO_PUBLIC_UPDATES_URL }}
      GH_TOKEN: ${{ github.token }}
      E2E_ACCOUNT_EMAIL: ${{ secrets.E2E_ACCOUNT_EMAIL }}
      E2E_ACCOUNT_PASSWORD: ${{ secrets.E2E_ACCOUNT_PASSWORD }}
    steps:
      - name: Checkout tested commit
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Maestro
        run: |
          set -euo pipefail
          curl -Ls "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> "$GITHUB_PATH"

      # The runner user cannot use /dev/kvm by default, and without it the emulator
      # falls back to software emulation and never finishes booting.
      - name: Enable KVM
        run: |
          set -euo pipefail
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # Fail fast, before paying for an emulator boot, when this commit needs a new
      # native distribution and no released APK can receive its OTA.
      - name: Resolve golden release
        id: golden
        run: |
          set -euo pipefail
          set +e
          node scripts/e2e/android.mjs --plan-only --abi x86_64
          status=$?
          set -e
          if [ "$status" -eq 0 ]; then
            echo "available=yes" >> "$GITHUB_OUTPUT"
          elif [ "$status" -eq 3 ]; then
            echo "::warning title=E2E skipped::No released APK is native-compatible with this commit. Ship a native distribution first; JS-only changes can ride an existing one."
            echo "available=no" >> "$GITHUB_OUTPUT"
          else
            exit "$status"
          fi

      # Every route, including app/debug, sits behind Logto (app/_layout.tsx renders
      # AuthScreen until a session exists). The production tenant signs in with an email
      # code only, which CI cannot read, and the dev tenant enables password sign-in.
      - name: Check E2E account
        id: account
        run: |
          set -euo pipefail
          if [ -z "${E2E_ACCOUNT_EMAIL:-}" ] || [ -z "${E2E_ACCOUNT_PASSWORD:-}" ]; then
            echo "::warning title=E2E skipped::No E2E account is configured. Add E2E_ACCOUNT_EMAIL and E2E_ACCOUNT_PASSWORD (dev tenant) to run the device flows."
            echo "ready=no" >> "$GITHUB_OUTPUT"
          else
            echo "ready=yes" >> "$GITHUB_OUTPUT"
          fi

      - name: Run device E2E
        if: steps.golden.outputs.available == 'yes' && steps.account.outputs.ready == 'yes'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          # API 35 keeps to system images the action builds reliably; the release APK
          # only needs its own minSdk satisfied.
          api-level: 35
          arch: x86_64
          target: google_apis
          emulator-boot-timeout: 900
          disable-animations: true
          script: node scripts/e2e/android.mjs --abi x86_64

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cohub-e2e-android-${{ github.run_id }}
          path: dist/e2e
          if-no-files-found: ignore
          retention-days: 14
